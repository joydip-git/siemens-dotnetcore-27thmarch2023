namespace InterfaceDemo.Entities
{
    //byte, sbyte, short, ushort, int (by default), uint, long, ulong are allowed as data types for the constant values assigned to the enum members
    public enum WriterOptions : long
    {
        Xml = 100, //=0
        Text = 200, //=1
    }
}
